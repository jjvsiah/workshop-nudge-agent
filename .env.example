# Model (Vercel AI Gateway) — required to run the agent
AI_GATEWAY_API_KEY=

# Pulled via `vercel link` + `vercel env pull` for Connect OIDC
# VERCEL_OIDC_TOKEN=

# Who receives workshop-nudge emails (you)
ALERT_EMAIL=you@example.com
ALERT_FROM_EMAIL=Workshop Nudge <onboarding@resend.dev>
RESEND_API_KEY=

# Optional: booking link included in alert emails
BOOKING_LINK=https://cal.com/your-handle/30min

# --- Snowflake via Vercel Connect (snowflake/account-intel) ---
SNOWFLAKE_ACCOUNT=kzympaw-mozartvercel
SNOWFLAKE_ROLE=REPORTER
SNOWFLAKE_WAREHOUSE=REPORTING
SNOWFLAKE_DATABASE=GTM
SNOWFLAKE_SCHEMA=ANALYTICS
SF_ACCOUNTS_TABLE=ACCOUNTS
# user | app — omit to auto-pick (user in interactive sessions, app for schedules)
# Deployed cron sets SNOWFLAKE_CONNECT_PRINCIPAL=app on Vercel.
# SNOWFLAKE_CONNECT_PRINCIPAL=app
# Force fixture demo data:
# SNOWFLAKE_USE_FIXTURES=1
# Password fallback:
# SNOWFLAKE_AUTH_MODE=password
# SNOWFLAKE_USERNAME=
# SNOWFLAKE_PASSWORD=
